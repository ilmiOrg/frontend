# Dependencies
node_modules/
package-lock.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*
own-react

# Production build (regenerate with `yarn build`)
build/

# Local env (never commit secrets)
.env
.env.local
.env.*.local

# Tooling / OS
coverage/
.DS_Store
*.log
.vscode/

# JetBrains
.idea/
*.iml

# CRA / tooling caches
.eslintcache

# Playwright screenshots + MCP logs (kept out of repo)
.playwright/
.playwright-mcp/
.yarn/
